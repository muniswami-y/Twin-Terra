# Twin-Terra Core Dependencies (Pinned for Scientific Reproducibility)
pandas>=2.0.0,<=2.2.3
numpy>=1.24.0,<=1.26.4
pytest>=7.4.0,<=8.3.4
pyproj>=3.6.0
shapely>=2.0.0

# Optional High-Throughput Raster / Vector Engines (Native C-Extensions)
# Install via pre-compiled wheels or conda if GDAL headers are present:
# rasterio>=1.3.8
# rioxarray>=0.15.0
# geopandas>=0.14.0
# richdem>=0.3.4
